# Sponsor Evidence State

| Capability | Current implementation | Claim boundary |
|---|---|---|
| Browserbase | Managed Session, server-side CDP, interactive Live View, explicit termination | Provider verified only after a WebAccessible-owned live session completes |
| EverOS | Live ownership adapter, profile/routine search, teach add/flush, skill and episode reads | Provider verified only after a real teach run returns retrievable memory |
| Snowflake | Live connector, migrations, outbox MERGE, Cortex guidance primitives, cost views, Streamlit | Provider verified only after backend-created rows are queryable |
| Beta Fund | Product and delivery attribution | Program context, not a runtime integration |
