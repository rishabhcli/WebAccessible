"""Caregiver-facing command line tools for WebAccessible.

The modules in this package are administration surfaces over the existing
provider adapters. They add no second memory model, no SDK calls of their own,
and no provider behaviour that the adapters do not already support.
"""
